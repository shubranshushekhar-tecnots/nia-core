{
  "name": "nia-core",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "bash scripts/check-workspace-scope-guard.sh && turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "lint": "turbo lint",
    "guard:scope": "bash scripts/check-workspace-scope-guard.sh",
    "migrate:status": "node scripts/migrate.mjs status",
    "migrate:push": "node scripts/migrate.mjs push",
    "migrate:verify": "node scripts/migrate.mjs verify"
  },
  "devDependencies": {
    "pg": "^8.13.1",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "vitest": "^5.0.0"
  }
}
